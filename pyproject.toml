[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "dynasub"
version = "0.1.0"
description = "Dynasub source transformation codec"
authors = [{ name = "Your Name" }]
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
